# Architecture

- **Front** : SPA légère (HTML + CSS + JS vanilla)
- **Core IA** : `script/ai-core.js` (orchestration des messages, appels API)
- **Design system** : `design/tokens.css`, `design/system.css`, `design/components.css`
- **Routing** : `script/router.js` (navigation /, /demo, etc.)
